# https://lefthook.dev/configuration/

pre-commit:
  parallel: true
  jobs:
    - name: eslint
      run: pnpm lint

    - name: typecheck
      run: pnpm exec tsc -b --noEmit

pre-push:
  jobs:
    - name: test
      run: pnpm test
